import { ConvertirEdadPipe } from './convertir-edad.pipe';

describe('ConvertirEdadPipe', () => {
  it('create an instance', () => {
    const pipe = new ConvertirEdadPipe();
    expect(pipe).toBeTruthy();
  });
});
